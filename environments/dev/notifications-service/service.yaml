apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  labels:
    app: notifications-service
    env: dev
spec:
  type: ClusterIP
  selector:
    app: notifications-service
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
