apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: ghcr-pull-secret
  namespace: default
spec:
  encryptedData:
    .dockerconfigjson: PLACEHOLDER_SEAL_AFTER_BOOTSTRAP
  template:
    type: kubernetes.io/dockerconfigjson
