apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
- rollout.yaml
- service.yaml
- analysis-template.yaml
- sealed-secret-ghcr.yaml

images:
- name: ghcr.io/itayna/java-sample-app
  # Promoted explicitly by .github/workflows/promote-prod.yml (never by CI).
  newTag: fd6a5e449a8e
