apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: __SERVICE__
  labels:
    app: __SERVICE__
    env: prod
spec:
  replicas: 3
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: __SERVICE__
  template:
    metadata:
      labels:
        app: __SERVICE__
        env: prod
    spec:
      imagePullSecrets:
      - name: ghcr-pull-secret
      containers:
      - name: app
        image: ghcr.io/__OWNER__/__SERVICE__:latest
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ENVIRONMENT
          value: "production"
        - name: APP_NAME
          value: "__SERVICE__ (prod)"
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          requests:
            cpu: 200m
            memory: 512Mi
          limits:
            cpu: 1000m
            memory: 1Gi
  strategy:
    canary:
      maxSurge: 1
      maxUnavailable: 0
      steps:
      - setWeight: 20
      - pause: {duration: 2m}
      - setWeight: 40
      - pause: {duration: 2m}
      - setWeight: 60
      - pause: {duration: 2m}
      - setWeight: 80
      - pause: {duration: 2m}
      canaryService: __SERVICE__-canary
      stableService: __SERVICE__
      analysis:
        startingStep: 1
        templates:
        - templateName: health-check
          clusterScope: true
        args:
        - name: service-name
          value: __SERVICE__-canary.default.svc.cluster.local
      abortScaleDownDelaySeconds: 30
